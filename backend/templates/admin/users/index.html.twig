{% extends 'base.html.twig' %}
{% block title %}Utilisateurs{% endblock %}
{% block body %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1>Utilisateurs</h1>
  <a class="btn btn-primary" href="{{ path('admin_users_new') }}"><i class="bi bi-plus"></i> Nouvel utilisateur</a>
  </div>
<table class="table table-sm align-middle">
  <thead><tr><th>#</th><th>Email</th><th>Rôles</th></tr></thead>
  <tbody>
  {% for u in users %}
    <tr>
      <td>{{ u.id }}</td>
      <td>{{ u.email }}</td>
      <td><code class="small">{{ u.roles|join(', ') }}</code></td>
    </tr>
  {% else %}
    <tr><td colspan="3" class="text-center text-muted">Aucun utilisateur</td></tr>
  {% endfor %}
  </tbody>
</table>
{% endblock %}
